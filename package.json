{
  "name": "orkes-conductor-docs",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "generate": "node scripts/generate-mkdocs-site.js",
    "fetch:oss-docs": "scripts/fetch-oss-docs.sh",
    "audit:docs": "node scripts/audit-docs-build.js",
    "build": "node scripts/generate-mkdocs-site.js && mkdocs build && node scripts/postprocess-mkdocs-build.js",
    "start": "node scripts/generate-mkdocs-site.js && mkdocs build && node scripts/postprocess-mkdocs-build.js && node scripts/serve-build.js",
    "serve": "node scripts/generate-mkdocs-site.js && mkdocs build && node scripts/postprocess-mkdocs-build.js && node scripts/serve-build.js"
  },
  "engines": {
    "node": ">=18.0"
  }
}
