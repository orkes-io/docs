---
sidebar_position: 6
slug: "/reference-docs/api/secrets/list-accessible-secrets"
description: "This API is used to retrieve all secret names that the user has permission to grant access to."
---

# List Accessible Secrets

**Endpoint**: `GET /api/secrets`

Retrieves all secret names that the user has permission to grant access to.

## Response

Returns an array of secret names (strings) that are accessible to the invoked user.

## Examples

<details>
<summary>List accessible secrets</summary>

**Request**

```shell
curl -X 'GET' \
  'https://<YOUR-SERVER-URL>/api/secrets' \
  -H 'accept: application/json' \
  -H 'X-Authorization: <TOKEN>'
```

**Response**

```json
[
  "openai_api_key",
  "github_token"
]
```

</details>